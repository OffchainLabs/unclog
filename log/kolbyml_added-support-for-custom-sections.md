### Added
- Support for overriding default changelog sections via a `.unclog.yaml` configuration file.

### Fixed
- `unclog check` now correctly fails when it encounters invalid sections instead of silently ignoring them.

### Changed
- Improved error messages for invalid sections to clearly list the problematic headers and provide valid alternatives.
